import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headerWrapper: {
        height: 80,
        backgroundColor: "#F17F3A",
        alignItems: 'center',
        justifyContent: 'center',
        paddingTop: 30
    },
    headerText: {
        color: "#fff",
        fontSize: 20,
        fontWeight: '600'
    }
})


export default styles;